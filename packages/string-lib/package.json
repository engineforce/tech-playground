{
  "name": "string-lib",
  "version": "0.0.1",
  "description": "String library",
  "main": "./dist/index.js",
  "author": "Paul Li",
  "license": "UNLICENSED",
  "scripts": {
    "build": "which poi && poi --version && poi build",
    "test": "tsc && eslint . && jest"
  },
  "dependencies": {
    "lodash": "^4.17.10"
  }
}

{
  "name": "aws-lambda-helpers",
  "version": "0.0.10",
  "description": "AWS Lambda helpers",
  "main": "./dist/index.js",
  "author": "Paul Li",
  "license": "UNLICENSED",
  "scripts": {
    "test": "jest",
    "build": "tsc && babel --ignore dist/ --ignore tests/ -d dist/ ./",
    "watch": "tsc -w"
  },
  "dependencies": {
    "clone-deep": "^4.0.0",
    "immutable-assign": "^2.0.6",
    "lodash": "^4.17.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.54",
    "@babel/core": "^7.0.0-beta.54"
  }
}
